#ifndef FUNCTION_H
#define FUNCTION_H

#include <utility>
#include <vector>
using std::pair;

//std::pair<int, int> min_max( int V[], std::size_t n );
std::pair<int,int> min_max( int V[], std::size_t n );

#endif
